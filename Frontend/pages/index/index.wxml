<!--index.wxml-->
<view class="container">
  <view class="map_container">
    <map class="bee-map" id="map" longitude="{{current_longitude}}" latitude="{{current_latitude}}" scale="13" show-location="true" bindmarkertap="makertap" circles='{{circle}}'></map>
  </view>
</view>